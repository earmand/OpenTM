# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'OpenTM' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for OpenTM
  pod 'CorePlot', '~> 2.2'
  pod 'Files', '~> 2.2.1'
  pod 'AEXML'
  #pod 'Kml.swift', '~> 0.3.0'
  pod 'Imaginary'


  target 'OpenTMTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OpenTMUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
